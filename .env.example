# ── Solana (Devnet) ─────────────────────────────────────────────
SOLANA_RPC=https://api.devnet.solana.com
# JSON byte-array keypair (solana-keygen new -o keypair.json --no-bip39-passphrase)
SOLANA_KEYPAIR=
# Deployed to Devnet (P1-T6)
VERITAS_PROGRAM_ID=CiGK2btZHdeW1U327ZLDhTQhDhP9TB6U16oG4a21YTUG

# ── Arc Testnet / Circle Gateway ────────────────────────────────
ARC_RPC=https://rpc.testnet.arc.network
# Buyer EVM private key (0x…, EOA — Gateway verifies with ecrecover) holding
# Arc Testnet USDC from https://faucet.circle.com (USDC is also Arc's gas).
# Used by the agent SDK / real-money E2E, never by the coordinator.
ARC_PRIVATE_KEY=
GATEWAY_API_URL=https://gateway-api-testnet.circle.com
# false = REAL Circle Gateway settlement on Arc Testnet (needs a buyer with a
# funded Gateway balance); true = mock provider (structural checks only).
MOCK_SETTLE=true
# Coordinator polls Gateway x402 transfers every N ms to advance settlement
# rows PENDING → AVAILABLE (0 = polling off; used when MOCK_SETTLE=false).
SETTLE_POLL_MS=0

# ── Coordinator ─────────────────────────────────────────────────
COORDINATOR_PORT=3001
# Credential seller middleware verifies on fan-out requests (MVP auth)
COORDINATOR_API_KEY=
VERITAS_FACILITATOR_URL=http://localhost:3001
# Basis-points fee on verified purchases
VERITAS_FEE_BPS=200

# ── Database ────────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/veritas

# Fee destination (Gateway address collecting Veritas fees)
VERITAS_FEE_ADDRESS=0x000000000000000000000000000000000000dEaD
